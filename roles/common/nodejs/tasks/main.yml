- name: Install nodenv
  command: nodenv install {{ item.version }} creates=~/.nodenv/versions/{{ item.version }}
  with_items:
      - {version: "v0.10.9"}
      - {version: "v0.11.9"}
      - {version: "v0.12.0"}
